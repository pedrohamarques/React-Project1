import ReactDOM from 'react-dom/client'; //

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
// it tells where the user interface should be placed in the webpage
// that is loaded

root.render(<App />);

// It's gonna be the first code that's gonna be read
